spring:
  application:
    name: auth-service

  cloud:
    nacos:
      discovery:
#        server-addr: nacos-service:8848
        server-addr: 127.0.0.1:8848
#        ip: 18.141.169.7
        enabled: true
        namespace: public

# 可选
server:
  port: 8083
