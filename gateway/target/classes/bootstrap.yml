spring:
  application:
    name: gateway

  cloud:
    nacos:
      discovery:
#        server-addr: nacos-service:8848
        server-addr: 127.0.0.1:8848
        enabled: true
        namespace: public

# 可选
server:
  port: 8080
  forward-headers-strategy: framework

