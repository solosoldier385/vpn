spring:
  application:
    name: user-service

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        enabled: true
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
#        shared-configs:
#          - data-id: common-mysql.yaml
#            group: LETSVPNSHARED_GROUP
#            refresh: true

# 可选
server:
  port: 8082
